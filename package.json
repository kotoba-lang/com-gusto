{
  "private": true,
  "scripts": {
    "test": "nbb --classpath src:test -e \"(require '[clojure.test :as t] 'gusto.main-test) (let [r (t/run-tests 'gusto.main-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
